% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Counterfactual.R
\name{counterfactual}
\alias{counterfactual}
\title{Define a post-calibration trade-policy counterfactual}
\usage{
counterfactual(
  ownership = NULL,
  costs = NULL,
  exit = NULL,
  capacity = NULL,
  tariff = NULL,
  quota = NULL,
  bargaining = NULL,
  leader = NULL,
  products = NULL,
  quality = NULL,
  entry = NULL,
  ...
)
}
\arguments{
\item{ownership}{Ownership changes, unsupported unless registered.}

\item{costs}{Cost changes, unsupported unless registered.}

\item{exit}{Products to remove, or a logical active-product vector.}

\item{capacity}{Capacity changes, unsupported unless registered.}

\item{tariff}{A post-counterfactual tariff vector.}

\item{quota}{A post-counterfactual quota vector.}

\item{bargaining}{Bargaining-parameter changes, unsupported unless registered.}

\item{leader}{Leader changes, unsupported unless registered.}

\item{products}{Product-structure changes, unsupported unless registered.}

\item{quality}{A named numeric vector of proportional attractiveness
changes keyed by product label. Logit adds `log1p(quality)` to the
utility; CES multiplies its positive mean-value weight by `1 + quality`.
Verified for every registered trade model whose legacy class wraps a
Logit/CES-family
  antitrust demand system (moncom, tariff Bertrand/Cournot, second-score
  auction, bargaining, and LogitCap-descended quota); unsupported for
  AIDS and linear/loglin Cournot tariff models, which have no `meanval`.}

\item{entry}{An entrant specification. Trade validates entry support when
the counterfactual is simulated for a fitted TradeFit.}

\item{...}{Reserved; model specification fields are rejected.}
}
\value{
A `Counterfactual` object with exactly one `CounterfactualStep`.
}
\description{
`counterfactual()` stores only supplied changes to the economic
environment, bundled as the single `CounterfactualStep` of a
`Counterfactual`. Demand, conduct, and model variants are deliberately
not counterfactual fields; use `update()` or `respecify()` for those
changes. Use `add_step()` to append further, sequential steps.
}
